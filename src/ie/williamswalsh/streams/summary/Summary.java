package ie.williamswalsh.streams.summary;

import java.util.Arrays;
import java.util.List;

public class Summary {
    public static void main(String[] args) {
        // findAny() -> Optional -> May not be the first element
        // findFirst() -> Optional -> When you specifically want the first element from the stream
        //     to find the first the stream needs to have an order

        // ifPresent()
        // isPresent()


        // filter -> intermediate

//        List<String> stringList = Arrays.asList("Hello", "World");
//        stringList.stream()
//                .forEach(System.out::println);
    }
}
